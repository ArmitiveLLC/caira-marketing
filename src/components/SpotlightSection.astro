---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className = '' } = Astro.props;
---

<section id={id} class:list={['spotlight-dark scroll-mt-24', className]}>
  <slot />
</section>
